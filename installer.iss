; Zero-file diagnostic installer — verifies ISCC itself runs correctly.
; If this fails, the problem is the ISCC invocation, not source files.
[Setup]
AppName=FL Agent
AppVersion=1.0.0
DefaultDirName={autopf}\FL Agent
DefaultGroupName=FL Agent
OutputDir=Output
OutputBaseFilename=FL_Agent_Setup
